# Nocky glass rule for Hyprland 0.53-0.54 (hyprlang).
# Load this after broader opaque/no_blur rules so the Nocky exception wins.
windowrule {
    name = nocky-glass
    match:class = ^(io\.github\.maylton\.Nocky)$

    no_blur = off
    opaque = off
    force_rgbx = off
    xray = off
}
